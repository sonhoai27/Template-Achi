import * as React from "react";
import {connect} from "react-redux";

class ClientSourceDetail extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <>
            </>
        )
    }
}

const mapStateToProps = storeState => ({});
const mapDispatchToProps = {};
export default connect(
    mapStateToProps,
    mapDispatchToProps
)(ClientSourceDetail);